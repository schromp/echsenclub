# Echsenclub

## TODO

### Migrate nginx to caddy

While were at that remove the proxy from sparrow and only use the one from cloudy.
That will mean that we always have to hop through cloudy though. And if cloudy goes down than we have a problem.
